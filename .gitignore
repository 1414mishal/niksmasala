# Node
node_modules/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
*.swp

# Claude internal
.claude/

# Large/sensitive files — do NOT ship to GitHub Pages
*.pptx
*.pdf
*.psd
*.ai
*.sketch
*.fig
*.zip
*.rar

# Originals (commit optimised .mp4 / .webp instead)
*.MOV
*.mov
# (existing IMG_*.MOV are already tracked — git rm them with --cached if you want to evict)

# Local env / keys
.env
.env.*
*.key
*.pem
secrets.json

# Experimental / scratch redesigns — never ship
experiment-design-apple/
experiment-design-premium/
experiment-design-revamp/
stitch_niks_masala_modern_redesign/
*.production-backup

# Unoptimised image originals at repo root (commit assets/*.webp instead)
/1.jpg
/2.png
/3.png
/a_professional_high_resolution_background_for_an_indian_spice_website._the.png
